<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Zudem'e Özel</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      color: white;
      overflow-x: hidden;
    }

    .login-screen {
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      backdrop-filter: blur(10px);
      text-align: center;
      padding: 20px;
    }

    .login-box {
      background: rgba(255,255,255,0.1);
      padding: 40px;
      border-radius: 25px;
      box-shadow: 0 0 25px rgba(0,0,0,0.2);
      width: 100%;
      max-width: 400px;
    }

    .login-box h1 {
      margin-bottom: 20px;
      font-size: 2rem;
    }

    .login-box input {
      width: 100%;
      padding: 15px;
      border: none;
      border-radius: 15px;
      margin-top: 15px;
      outline: none;
      font-size: 1rem;
    }

    .login-box button {
      margin-top: 20px;
      padding: 15px;
      width: 100%;
      border: none;
      border-radius: 15px;
      background: white;
      color: #ff4f81;
      font-size: 1rem;
      cursor: pointer;
      transition: 0.3s;
      font-weight: bold;
    }

    .login-box button:hover {
      transform: scale(1.05);
    }

    .hidden {
      display: none;
    }

    .main-site {
      padding: 30px;
      text-align: center;
      animation: fadeIn 1s ease;
    }

    .hero {
      margin-top: 40px;
    }

    .hero h1 {
      font-size: 3rem;
      margin-bottom: 20px;
    }

    .hero p {
      max-width: 700px;
      margin: auto;
      line-height: 1.8;
      font-size: 1.2rem;
    }

    .gallery {
      margin-top: 60px;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
    }

    .gallery img {
      width: 100%;
      height: 300px;
      object-fit: cover;
      border-radius: 20px;
      transition: 0.4s;
      box-shadow: 0 0 20px rgba(0,0,0,0.2);
    }

    .gallery img:hover {
      transform: scale(1.03);
    }

    .message-box {
      margin-top: 70px;
      background: rgba(255,255,255,0.12);
      padding: 35px;
      border-radius: 25px;
      max-width: 850px;
      margin-left: auto;
      margin-right: auto;
      line-height: 2;
      font-size: 1.15rem;
    }

    .footer {
      margin-top: 70px;
      padding-bottom: 40px;
      font-size: 1rem;
      opacity: 0.9;
    }

    .heart {
      position: fixed;
      color: white;
      font-size: 20px;
      animation: floatUp 6s linear infinite;
      opacity: 0.7;
    }

    @keyframes floatUp {
      from {
        transform: translateY(100vh) scale(0.5);
        opacity: 1;
      }
      to {
        transform: translateY(-10vh) scale(1.5);
        opacity: 0;
      }
    }

    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.1); }
      100% { transform: scale(1); }
    }

    @keyframes fadeOut {
      to {
        opacity: 0;
        visibility: hidden;
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>

  <div class="login-screen" id="loginScreen">
    <div class="login-box">
      <h1>🔐 Sadece Zudeme Özel</h1>
      <p>Bu sayfa sadece bizim için hazırlandı ❤️</p>
      <input type="password" id="password" placeholder="Şifreyi gir" />
      <button onclick="checkPassword()">Giriş Yap</button>
    </div>
  </div>

  <div class="main-site hidden" id="mainSite">
    <div id="intro" style="position:fixed;inset:0;background:linear-gradient(135deg,#ff758c,#ff7eb3);display:flex;justify-content:center;align-items:center;z-index:9999;flex-direction:column;text-align:center;animation:fadeOut 2s ease 4s forwards;">
    <h1 style="font-size:4rem;">Fırat ❤️ Zudem</h1>
    <p style="margin-top:20px;font-size:1.3rem;">Sonsuza kadar bizim hikayemiz...</p>
  </div>

  <div class="hero">
      <h1>İyi ki Doğdun Aşkım ❤️</h1>
      <p>
        Hayatıma girdiğin günden beri her şey daha güzel oldu.
        Senin gülüşün, sesin, mutlu olduğun anlar benim en değerli şeyim.
        Bu küçük siteyi sana olan sevgimi göstermek için hazırladım.
      </p>
    </div>

    <div style="margin-top:50px;font-size:1.3rem;font-weight:bold;">
      ⏳ Seninle geçen her saniye benim için bir ömür gibi güzel...
    </div>

    <div class="gallery">
      <img src="foto1.jpg" alt="Fotoğraf 1">
      <img src="foto2.jpg" alt="Fotoğraf 2">
      <img src="foto3.jpg" alt="Fotoğraf 3">
      <img src="foto4.jpg" alt="Fotoğraf 4">
    </div>

    <div style="margin-top:60px;">
      <button onclick="showLetter()" style="padding:15px 30px;border:none;border-radius:20px;background:white;color:#ff4f81;font-size:1rem;font-weight:bold;cursor:pointer;">
        💌 Sana Bir Mektubum Var
      </button>
    </div>

    <div id="letterBox" style="display:none;margin-top:30px;background:rgba(255,255,255,0.12);padding:30px;border-radius:25px;max-width:800px;margin-left:auto;margin-right:auto;line-height:2;font-size:1.1rem;">
      Bazen sadece seni düşünmek bile yüzümü gülümsetmeye yetiyor.
      Hayatımın en güzel anıları seninle oluştu.
      Sen benim huzurumsun, mutluluğumsun, en güzel tesadüfümsün.
      İyi ki doğdun aşkım... ❤️
    </div>

    <div class="message-box">
      Seninle geçen her an benim için çok değerli.
      Bazen sadece sana bakarken bile içim huzur doluyor.
      Hayatımda olduğun için şanslı hissediyorum.
      Ne olursa olsun seni sevmekten vazgeçmeyeceğim.
      Senin mutlu olman benim için her şeyden daha önemli.
      <br><br>
      İyi ki varsın Zudem... ❤️
    </div>

    <div style="margin-top:70px;">
      <div onclick="surpriseMessage()" style="font-size:5rem;cursor:pointer;animation:pulse 1.5s infinite;">
        🎂
      </div>
      <p style="margin-top:15px;">Pastaya dokun 🎉</p>
    </div>

    <div class="footer">
      Sonsuza kadar sen ve ben 💖
    </div>
  </div>

  <script>
    const correctPassword = "1301";

    function checkPassword() {
      const entered = document.getElementById("password").value;

      if (entered === correctPassword) {
        document.getElementById("loginScreen").classList.add("hidden");
        document.getElementById("mainSite").classList.remove("hidden");
      } else {
        alert("Şifre yanlış ❤️");
      }
    }

    function createHeart() {
      const heart = document.createElement("div");
      heart.classList.add("heart");
      heart.innerHTML = "❤";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = (Math.random() * 3 + 3) + "s";
      heart.style.fontSize = (Math.random() * 20 + 10) + "px";
      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 6000);
    }

    setInterval(createHeart, 300);

    function showLetter() {
      const box = document.getElementById("letterBox");
      box.style.display = box.style.display === "block" ? "none" : "block";
    }

    function surpriseMessage() {
      alert("🎉 İyi ki doğdun aşkım! Seni sonsuz kere seviyorum ❤️");
    }
  </script>

  <audio id="music" autoplay loop>
    <source src="muzik.mp3" type="audio/mpeg">
  </audio>

  <div id="countdown" style="position:fixed;top:15px;right:15px;background:rgba(255,255,255,0.15);padding:15px 20px;border-radius:20px;font-weight:bold;backdrop-filter:blur(10px);z-index:999;">
    ❤️ Birlikte geçen gün: <span id="daysTogether"></span>
  </div>

  <script>
    const startDate = new Date("2026-01-13");
    const today = new Date();
    const diffTime = today - startDate;
    const diffDays = Math.floor(diffTime / (1000 * 60 * 60 * 24));
    document.getElementById("daysTogether").innerText = diffDays;

    document.addEventListener("click", () => {
      document.getElementById("music").play();
    });
  </script>

</body>
</html>
